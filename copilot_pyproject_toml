[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "jm_utils"
version = "0.1.0"
description = "A utility package for data science workflows including core tools, data manipulation, and plotting utilities."
authors = [
    { name="Jorge", email="jorge@example.com" }
]
license = {text = "MIT"}
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "numpy>=1.21",
    "pandas>=1.3",
    "matplotlib>=3.4",
    "seaborn>=0.11",
    "rich>=12.0"
]

[project.urls]
Homepage = "https://github.com/jorge/jm_utils"
Repository = "https://github.com/jorge/jm_utils"

[tool.setuptools.packages.find]
where = ["core", "data", "pwd"]
include = ["jm_utils.core*", "jm_utils.data*", "jm_utils.pwd*"]

[tool.setuptools]
package-dir = {"" = "."}